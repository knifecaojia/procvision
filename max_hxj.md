# 05ui-poc 项目说明

## 这是什么项目

这个项目可以把它理解成一套“车间现场用的工业视觉工作台”。

它不是一个普通的网页后台，也不是一个单纯的图像识别脚本，而是一套在本地电脑上运行的桌面系统。操作员登录后，可以连接相机、查看工艺步骤、对照引导图完成装配，再由算法或人工去判断当前步骤是否通过，最后把结果保存并上报。

如果把整个系统比作一个有分工的生产小组：

- `ui` 像前台指挥台，负责把信息清楚地展示给人，并接收人的操作。
- `camera` 像现场摄像师，负责把画面稳定送进来。
- `runner` 像算法执行员，接到某一步的任务后去跑识别流程。
- `services` 像后勤组，负责记录结果、整理图片、上传信息、留痕追踪。
- `core` 像值班室，负责启动应用、读取配置、维持运行秩序。
- `auth` 像门禁，负责登录和会话。

这个分工是合理的，因为工业现场最怕“看起来能跑，出问题时找不到是谁的锅”。拆分清楚以后，定位问题会快很多。

## 这个项目为什么用桌面程序

项目采用的是 `PySide6`，也就是基于 Qt 的桌面应用方案，而不是前后端分离网页。

这背后有很现实的原因：

- 现场设备通常是工业 PC，本地接相机、本地跑流程，本地桌面程序更直接。
- 相机、算法、图片显示、窗口交互这些事情，桌面方案更顺手。
- 现场常常有离线环境，网页方案会多一层浏览器和部署复杂度。
- 工艺执行页面对“稳定、清晰、响应快”要求很高，Qt 在这类场景里比较成熟。

简单说，这个项目不是为了追求“新潮”，而是为了让现场操作员少折腾、让维护人员少背锅。

## 项目结构怎么理解

项目顶层大致可以这样看：

- `run_app.py`
  - 程序入口，应用从这里启动。
- `build.spec`
  - 打包成 EXE 时的说明书。
- `config.json`
  - 运行配置，总开关很多都在这里。
- `src/`
  - 主要代码都在这里。
- `data/`
  - 放模拟数据、相机预设等运行数据。
- `config/`
  - 放运行期配置文件。

`src/` 里面最重要的几块如下：

- `src/core/`
  - 应用初始化、配置读取、路径管理、会话等基础能力。
- `src/ui/`
  - 所有界面相关代码。
- `src/camera/`
  - 相机连接、相机管理、预设、标定。
- `src/runner/`
  - 算法执行引擎，负责真正调度检测流程。
- `src/services/`
  - 结果记录、网络上报、检测图标注、追踪日志等。
- `src/auth/`
  - 登录认证。

其中，和这次问题最相关的是：

- `src/ui/windows/process_execution_window.py`
  - 工艺执行主窗口，负责串起整个执行过程。
- `src/ui/windows/auto_detect_mixin.py`
  - 自动检测循环逻辑，控制开始、停止、重试、推进下一步。
- `src/ui/windows/ui_builder_mixin.py`
  - 底部按钮区、状态区等界面构建逻辑。

这三个文件一起，构成了“用户看到的按钮”和“系统内部实际状态”之间的桥梁。

## 工艺执行窗口是怎么组织的

这个窗口没有把所有事情都堆在一个超大文件里，而是采用了 Mixin 拆分。

可以把它理解成一个主导演带几个副导演：

- `process_execution_window.py`
  - 负责主线剧情，比如当前步骤是谁、什么时候进入下一步、窗口何时关闭。
- `ui_builder_mixin.py`
  - 负责搭舞台，把按钮、步骤条、状态区这些东西拼出来。
- `camera_mixin.py`
  - 负责接相机、收画面、停预览。
- `guide_image_mixin.py`
  - 负责引导图下载、缓存和显示。
- `detection_mixin.py`
  - 负责手动检测流程。
- `auto_detect_mixin.py`
  - 负责自动检测流程。

这种拆法的优点很明显：

- 每份代码关注一个主题，阅读成本低一些。
- 查问题时可以先缩小范围。
- 新人接手时，不容易被一个上千行文件直接劝退。

但它也有一个隐含要求：不同文件之间的状态约定必须非常严格。只要约定松了，就容易出现“这边以为停了，那边以为还在跑”的问题。这正是本次故障的核心教训。

## 一次完整检测大概怎么流转

从业务角度看，一次流程大概是这样的：

1. 用户登录系统。
2. 进入工艺执行页面。
3. 连接相机，画面进入预览区。
4. 系统根据工艺步骤显示当前指导信息和引导图。
5. 用户选择手动检测或自动检测。
6. 画面和步骤信息被送入算法执行。
7. 算法返回通过、不通过或报错。
8. 系统把结果显示出来，并保存图片、记录、上报结果。
9. 如果通过，就进入下一步；如果不通过，可能重试或等待处理。

看起来像一条直线，但真正难的地方在于：这里面同时有用户点击、后台线程、定时器、页面刷新、步骤切换。也就是说，它不是“先做完 A 再做 B”那么简单，而是多个时钟同时在走。

## 自动检测为什么容易出状态问题

自动检测和手动检测最大的区别，不在于“是不是自动”，而在于它会自己往下推进。

自动检测里通常同时存在这些东西：

- 当前是否开启自动模式
- 当前是否正在检测
- 当前结果是通过、不通过还是报错
- 是否准备自动重试
- 是否准备自动进入下一步
- 页面上按钮应该显示什么

这些状态看起来都差不多，但它们其实不是一回事。

举个通俗一点的比喻：

“自动检测是否开启”像是汽车有没有挂上巡航模式；
“当前是否正在检测”像是发动机这一秒是不是正在做功。

巡航关掉了，不代表发动机状态就自动恢复到停车；反过来也一样。

本次故障，恰恰就是这两个概念在某条停止路径上没一起收尾，结果就出现了：

- 系统内部已经认为自动模式关了
- 但界面仍然认为当前还在检测

于是页面就卡在一个很尴尬的中间态：两个按钮都灰了，谁也救不了场。

## 这次问题是怎么发生的

这次用户人工测试发现，在自动检测模式下反复“开始 / 停止”后，有概率出现：

- 页面底部还写着“自动检测中...”
- 左侧按钮显示“检测中”
- 右侧按钮显示“自动检测”
- 两个按钮都灰色不可点击

这张现象图其实非常关键，因为它不是普通的“界面不对”，而是把内部矛盾直接露出来了。

如果自动模式真的还开着，右侧按钮应该显示“停止自动”；
如果当前检测真的已经结束，左侧按钮就不该还显示“检测中”。

现在这两个现象同时出现，说明系统里至少有两组状态没有对齐：

- 一组说：自动模式已经退出
- 另一组说：检测还在进行中

经过检查，问题根因落在自动检测停止路径上：

- 停止逻辑确实把自动检测控制器停掉了
- 但没有同步把页面的“检测中”状态一起复位

所以当底部状态区重新绘制时，它拿到的就是一组互相打架的数据，于是就把两个按钮都画成了不可点击的样子。

## 这次修复做了什么

这次修复没有去“硬放开按钮”，也没有去碰按钮样式，而是直接回到根上处理。

修复思路很朴素：

- 自动检测开始时，由控制器和主窗口保持一致的开启状态。
- 自动检测停止时，不只是停线程和定时器，还要把页面一起收回到空闲态。
- 主窗口不再自己提前改一半状态，而是把“我要停止”这件事交给统一的停止逻辑处理。

这样做的结果是：

- 用户点“停止自动”后，页面不再残留“检测中”状态。
- 底部按钮会回到可操作状态。
- 出错停止、最后一步停止、关闭窗口等路径，也更不容易再漏掉某一步状态同步。

## 为什么这次修法比“放开按钮”更好

一个常见但危险的修法是：

“既然按钮灰了，那我就改按钮条件，让它别灰。”

这种修法看起来很快，实际上像是把仪表盘胶带贴住。

因为真正的问题不是按钮本身，而是按钮读到的状态是错的。

如果只改按钮规则，短期可能看起来能点了，但后台仍可能保留错误状态，后续还会在别的地方炸出来。比如：

- 自动重试仍偷偷在跑
- 下一步推进时序错乱
- 页面提示和真实执行状态继续不一致

好的修复，不是让症状看起来消失，而是让系统重新说同一种语言。

## 这个项目里值得记住的工程经验

### 1. 界面状态不是业务状态的“装饰”

很多人会下意识觉得，UI 只是显示层，出问题时先看界面怎么画。

但在这种桌面交互系统里，UI 本身就是状态机的一部分。按钮是否可点、文案写什么、引导图是否切换，这些都会影响用户下一步操作。

如果业务状态和界面状态不同步，用户就会把系统带进更奇怪的分支里。

### 2. 退出路径最容易漏逻辑

开始流程往往写得很完整，因为大家都会盯着“怎么跑起来”。

真正容易漏的是这些地方：

- 用户中途停止
- 后台线程报错
- 自动重试还没触发就被打断
- 最后一步完成后自动收尾
- 窗口直接关闭

经验上，凡是“退出路径”都要特别小心。它们不像主路径那样天天被走到，但一旦漏掉，问题通常又难复现、又难解释。

### 3. 两套“看起来差不多”的状态，迟早会打架

本次问题里，“自动模式是否开启”和“当前是否处于检测中”就是两个不同概念。

它们都存在是正常的，但必须非常清楚：

- 谁是事实来源
- 谁负责改它
- 哪些退出路径必须同时更新它们

如果没有统一出口，后面哪怕只多一个新分支，也很容易让它们再次脱节。

### 4. 截图不是附属材料，有时是强证据

这次补充的 UI 截图之所以有价值，不只是因为“看到了 bug”，而是因为它把几个按钮的组合状态同时展示出来了。

优秀的排查，不会只把截图当作陪衬，而是会把截图和代码里的状态规则逐项对照。很多时候，截图能帮我们比日志更快地缩小范围。

## 以后维护这个项目，建议优先关注什么

如果后续再遇到“按钮不对、文案不对、流程像卡住”的问题，建议优先按这个顺序查：

1. 当前页面上有哪些可见信号
   - 文案是什么
   - 哪些按钮可点
   - 哪些按钮隐藏了
2. 这些可见信号分别由哪些状态控制
3. 这些状态是谁改的
4. 有没有某条退出路径只改了一部分
5. 是否存在后台线程、定时器、异步回调晚到的情况

这个顺序的好处是，不会一上来就扎进细节泥潭，而是先把“现象 -> 状态 -> 改状态的人”这条线串起来。

## 这个项目的技术取舍，有哪些值得借鉴

我认为这个项目有几处取舍是比较成熟的：

- 采用本地桌面应用，贴合工业现场现实条件。
- 把相机、算法、界面、记录分层，职责边界比较清楚。
- 工艺执行窗口按主题拆分，降低了理解门槛。
- 结果图绘制、记录保存、结果上报逐渐往服务层收拢，这是对的方向。

当然，也有需要持续警惕的地方：

- 状态字段一多，就容易散落在不同文件里。
- Mixin 拆分虽然减轻了单文件压力，但也提高了跨文件协作的要求。
- 一旦异步流程增多，任何“顺手写一行”的状态修改，都可能埋下后患。

## 如果把这个项目交给新人，我会怎么讲

我不会先讲类图，也不会先从配置文件背起。

我会先让他明白一句话：

“这是一套围绕工艺步骤运行的本地工业视觉工作台，核心是让人、相机、算法和结果记录在同一个节奏里协作。”

然后我会让他先看这几条主线：

- 程序怎么启动
- 工艺执行页面怎么进入
- 一步检测怎么发起
- 结果怎么显示和保存
- 自动检测怎么开始、怎么停止、怎么推进下一步

只要这几条主线通了，再去看具体页面、服务和细节实现，就不会迷路。

## 这次相机绑定问题给我们的提醒

这次又暴露出另一类很典型、也很容易在工业现场翻车的问题：

不是“连不上相机”，而是“连上了，但可能连错了相机”。

这比单纯报错更危险。因为报错会让人停下来检查，连错了反而可能带着错误结果继续往下走。

局域网里如果有多台装了本项目的客户端，又有多台海康相机，系统就不能再把“相机名字”当成身份证。名字只是挂牌子，挂牌子可以重复，甚至可以被人改掉。真正靠谱的，是海康 SDK 枚举出来的稳定设备身份，比如序列号；IP 地址更像门牌号，查找很方便，但门牌号会调整，所以更适合作为辅助确认，而不是唯一依据。

这件事很像仓库领工具：

- 工具箱外面贴的“1号扳手”标签，可能会掉、会贴错。
- 仓库位置也可能调整。
- 但工具本体上的钢印编号，才是最稳的。

所以这次我们把思路收敛成一条很朴素但很重要的原则：

- 用序列号认设备
- 用 IP 帮现场确认
- 用本机绑定记录决定“这台电脑应该连谁”
- 用海康 SDK 的独占访问能力防止多台客户端同时抢同一台相机

这里面的工程教训也很值得记住：

- 能展示给人的字段，不一定能拿来做系统判定。
- “当前扫到了什么” 和 “这台机器应该用谁” 不是一回事，必须分层。
- 枚举顺序这种看似方便的东西，通常只适合临时显示，不适合做长期身份。
- 工业现场最怕的不是明显报错，而是悄悄跑偏。

## 最后总结

这个项目最有价值的地方，不是“它能跑视觉算法”，而是它把现场操作、画面预览、工艺步骤、检测判断和结果沉淀串成了一条完整链路。

而这次自动检测按钮卡死的问题，也再次提醒我们：

在这类系统里，最难的从来不是“做一个按钮”，而是让按钮背后代表的状态始终可信。

把这件事做好，系统就稳。
把这件事做松，现场就会被莫名其妙的小概率问题折腾得很痛苦。

## 这次任务物料提醒改造，说明了什么

最近这轮改造，表面上看只是给工艺执行流程补两块界面：

- 启动前先弹出物料确认
- 执行中右侧显示工艺文件和物料信息

但它背后真正做的事，其实是把“任务接口里的新字段”正式接进系统主流程。

如果把原来的流程比作一张工单在车间里流转，那么以前客户端拿到的主要还是：

- 任务号
- 工艺号
- 工序号
- 步骤列表

现在则多了两类对现场真正有帮助的信息：

- `process_desc`
  - 让人知道这一道工序到底是干什么
- `material_list`
  - 让人知道这一道工序要看哪些料、哪些料容易出错

这件事很重要，因为工业现场最怕一种情况：系统说“可以开始”，但人还没来得及核对关键物料。

所以这次我们没有把物料信息只是塞到执行页里“顺便看看”，而是故意拆成两段：

### 第一段：启动前提醒

只要任务带了 `material_list`，点击启动时先弹确认框。

这样做的价值很直接：

- 人在真正进入检测流程前，先看一眼关键物料
- 有 `error_prevention_mark` 的项会被突出显示
- 现场最容易出错的料，不会被埋在执行中途才看到

这就像开工前先核对工具和料盒，而不是螺丝拧到一半才发现拿错件。

### 第二段：执行中持续展示

进入工艺执行窗口后，右侧继续保留一块固定信息区，持续显示：

- 生产订单号
- 工艺编码、版本、名称
- 工序编码、名称、描述
- 物料清单

这样做，是为了避免操作员只能靠记忆做事。人眼盯着相机画面和步骤变化时，很容易忘掉上一秒刚确认过的物料信息。右侧固定区的作用，就是让这些信息一直在旁边，不需要来回切页查。

后来我们又往前走了一步：这块右侧信息区现在支持折叠收起。

这不是“做个动画看起来高级”，而是很实在的现场取舍。

因为执行窗口里，中间画面和步骤才是操作员最盯着看的东西。右侧工艺和物料信息很重要，但不是每一秒都需要占满那么宽的区域。所以现在把它做成了两种状态：

- 需要核对信息时，展开完整面板
- 需要把中间视觉区域让出来时，收成右侧窄边栏

这样处理比“直接删掉面板”更好，因为信息还在，只是暂时让路。

## 为什么这次特地加了一层“字段适配层”

这次改造里，一个很关键但不显眼的决定，是新增了 `src/services/task_payload_mapper.py`。

原因很现实：

- 旧接口字段和新接口字段一度并存
- 页面层以前已经自己写了一些兼容逻辑
- 如果这次继续在页面里东拼一点、西补一点，后面只会越来越乱

所以现在的思路是：

- 任务列表页不要自己解释一堆字段
- 执行窗口也不要直接猜服务端返回结构
- 所有“这字段叫什么、空值怎么处理、旧字段怎么兼容”的事，都收口到一个地方

这就像工厂里设了一个总装前的分拣台。前面来的箱子标签可能不完全一样，但进正式工位之前，先在分拣台统一整理好，后面的人就不用边干边猜。

这次字段适配里，最值得记住的几条规则是：

- 物料字段直接沿用接口正式名字，不再自己起别名
- 空值统一显示成 `-`
- `error_prevention_mark` 只要非空，就按“需要重点提示”处理

这三条看起来很朴素，但非常实用。因为它们能让前端在后端值域继续演进时，少改很多地方。

## 一个很典型的工程教训：字段有了，不代表数据就完整了

这次联调过程中，其实还出现了一个很常见的阶段性状态：

- 接口结构已经补好了
- 但有些预留字段实际值还是 `null`

比如：

- `assembly_number`
- `position_number`
- `model_no`
- `polarity_direction`

这类情况很像仓库货架的标签已经贴上了，但货还没完全摆进去。

如果工程上处理得不好，就会出现两种坏味道：

- 要么前端因为字段为空直接报错
- 要么前端干脆不显示这些列，等后面值来了又得重新改界面

这次我们选择的是第三条路：

- 字段位置先保留
- 没值就显示 `-`
- 一旦后端开始回真实值，界面自然就能接住

这是非常值得保留的习惯。因为它让系统面对“接口已经稳定、数据还在逐步完善”的中间阶段时，依然能稳稳工作。

## 为什么隐藏 mock 入口不能被新规则误伤

项目里有一个很有现场味道的设计：双击“装配引导与检测”标题，会直接进入隐藏 mock 流程。

这个入口的意义不是花哨，而是给开发和联调留一条不影响正式任务的快速通道。

所以这次虽然给正式任务加了“启动前先看物料”的新门槛，但 mock 流程不能被它误拦住。否则开发调试时，每次都要被正式逻辑绊一下，效率会明显下降。

这个点很像工厂里给设备维修留的旁路：

- 正式生产有正式生产的流程
- 调试和维护要有自己的快速通道

如果所有通道都强行走一条线，表面上看统一，实际上会把调试效率拖垮。

## 为什么又补了 `MOCK CAM`

这次为了把 18 和 30 的新效果更顺手地联调，我们又往隐藏 mock 流程前走了一步：

- 普通双击标题，进入“有物料 mock”
- `Shift + 双击` 标题，进入“无物料 mock”
- 如果没有真实相机，中间画面区直接显示 `MOCK CAM`
- 但流程不被阻断，仍然可以继续模拟检测

这背后的思路很务实：

18 和 30 主要看的是“启动前有没有提醒”“执行中右侧有没有信息”，它们本质上不是相机能力问题。如果为了看这两块界面，还必须先找一台真实相机接上，那联调成本就太高了。

所以这次我们把 mock 流程做成了真正的“本地演练通道”：

- 假任务数据可以自己带
- 假物料可以自己造
- 假步骤也可以完整走
- 相机没有也能跑
- 但正式任务仍然保持真实约束

而且这次还顺手修掉了一个很容易让人误判的小坑：

- 隐藏 mock 虽然造了步骤
- 但中间经过一次任务数据转换时，步骤字段可能被丢掉
- 结果就是“看起来进了 mock 流程，但步骤体验不完整”

这次把这个链路也补齐后，mock 现在不只是能打开页面，而是真的能带着工艺步骤往后走。这样你在本地做联调时，看到的节奏会更接近真实任务。

这件事的价值，不只是方便开发，而是让系统有了更像样的“演练模式”。

在工程实践里，好的 mock 不是“能打开个页面就算了”，而是要尽量复用真实流程，只把外部依赖换成可控的假输入。这样你在本地看到的行为，才更接近线上真实情况。

## 这次改造能带来的长期好处

这轮改造完成后，系统会多出几层更扎实的能力：

### 1. 接口升级不再直接冲击页面

因为字段处理被集中收口，后面接口如果再调整，波及面会小很多。

### 2. 任务启动更符合现场习惯

先确认物料，再进入执行，比“一点就进检测”更贴近真正的装配场景。

### 3. 执行页面的信息密度更合理

中间区域继续专注看画面，右边专注看工艺和物料，不会把所有东西糊在一层。

### 4. 项目更容易继续长大

以后如果还要接：

- 物料图片
- 物料来源批次
- 特殊装配备注
- 反装提醒

这次搭好的结构都还能继续承载，不需要重新推翻。

## 最后一句话

这次改造再次说明一个很朴素的事实：

好的系统，不只是“接口通了、页面能显示”，而是要让数据、流程和人的操作节奏真正对上。

物料提醒这件事，看起来像一个小功能，实际上是在把系统从“能跑流程”往“更懂现场”再推一步。
